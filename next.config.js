/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Remove appDir - it's enabled by default in Next.js 13.4+
  },
  // Add other config options if needed
}

module.exports = nextConfig